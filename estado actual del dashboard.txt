# secuencia, un comando en cada terminal.

nohup python simple_broker.py > simple_broker.out 2>&1 &

# Crear una sesión
screen -S mi_agente

# Dentro de screen ejecutar normalmente
sudo python promiscuous_agent.py

# Desconectarte con Ctrl+A, D
# Reconectarte después con: screen -r mi_agente

nohup python binary_monitor.py > binary_monitor.out 2>&1 &

nohup python ml_detector_5560.py > ml_detector_5560.out 2>&1 &

nohup python protobuf_gis_dashboard.py > protobuf_gis_dashboard.out 2>&1 &

