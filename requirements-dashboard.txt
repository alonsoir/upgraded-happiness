# Dashboard V3.1 ETCD - Minimal dependencies
# Only what dashboard_v31_etcd.py actually needs

# Core messaging and async
pyzmq>=25.1.0,<26.0.0

# System monitoring
psutil>=5.9.0,<6.0.0

# Web framework
flask>=2.3.0,<4.0.0
flask-cors>=4.0.0,<5.0.0

# etcd client and crypto
etcd3>=0.12.0,<1.0.0
protobuf>=3.19.0,<4.0.0

# Only if dashboard uses these (verify in code)
pyyaml>=6.0,<7.0
requests>=2.28.0,<3.0.0